{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="header clearfix">
            <h3 class="text-muted">Import Csv with a curl call to REST api</h3>
        </div>

        <div class="jumbotron">
            <table class="table table-sm table-striped">
                <thead>
                <tr class="table-success">
                    {% for key in adherents[1]|keys %}
                        <th scope="col">{{ key }}</th>
                    {% endfor %}
                </tr>

                </thead>
                <tbody>
                {% for adherent in adherents %}
                    <tr>
                        {% for key,value in adherent %}
                            <td>{{ value }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <footer class="footer">
            <p>©Alexandre Joulie 2018</p>
        </footer>
    </div>
{% endblock %}